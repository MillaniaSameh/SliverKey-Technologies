@page
@model IndexModel

@if (User.Identity.IsAuthenticated)
{
    @if (User.IsInRole("Admin"))
    {
        @Html.Partial("_NewContactForm")
    }
    else 
    {
        Response.Redirect("/ContactsList");
    }
}
else
{
    @Html.Partial("_LoginForm")
}